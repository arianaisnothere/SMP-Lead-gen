// For production deployment with Coolify or other hosting services,
// replace the URL below with your actual n8n webhook endpoint
const config = {
    N8N_WEBHOOK_URL: 'https://n8n.solar-alliance.com/webhook/smp-lp'
};

export default config;